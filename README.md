# my-room-skills

This repo is the **built output** of a personal portfolio site — not the
source code. It's published here automatically (`npm run deploy`) purely
so GitHub Pages can serve it publicly while the actual source stays in a
private repository.
